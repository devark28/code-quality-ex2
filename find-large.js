/**
 * Find the largest number among 3 numbers
 * 
 * @param {number} a
 * @param {number} b
 * @param {number} c
 * @returns {number}
 */
export function findLarge(a, b, c) {
    return Math.max(a, b, c);
}
